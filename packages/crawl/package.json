{
  "name": "@openllmrank/crawl",
  "version": "0.1.0",
  "description": "Crawlability check engine: SSRF-guarded crawler, robots/bot-roster analysis, and indexability findings shared by the hosted web check and the worker.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./describe": "./src/describe.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run bin/crawl-check.ts"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0",
    "robots-parser": "^3.0.1",
    "zod": "^3.25.0"
  }
}
